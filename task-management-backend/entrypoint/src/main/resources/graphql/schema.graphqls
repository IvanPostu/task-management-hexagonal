scalar Instant

type Query {
    getEmployee(id: ID) : Employee
    getAllStores: [Store]
}

type Store {
    id: ID!
    name: String
    city: String
    createDate: Instant!
}

type Employee {
    id: ID!
    name: String!
    location: Store
}
